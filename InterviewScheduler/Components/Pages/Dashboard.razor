@page "/dashboard"

@using Microsoft.AspNetCore.Authorization

@attribute [Authorize]

<PageTitle>Dashboard</PageTitle>

<h1>You are authenticated to a interview scheduler dashboard</h1>

<AuthorizeView>
    Hello @context.User.Identity?.Name!
</AuthorizeView>